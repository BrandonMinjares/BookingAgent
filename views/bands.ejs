<% include partials/header %>

<div class = "container-fluid">
    <div class ="row">
        <div class="col">
            <% bands.forEach(function(band) { %>
                <div>
                <h4><%= band.name %></h4>
                </div>
            <% }); %>
        </div>
    </div>
</div>

<% include partials/footer %>
